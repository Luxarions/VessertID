/**
 * device.js
 * Changes when: the device registry changes.
 * @memberof Vessert
 */

/** @memberof Vessert @namespace */
const Device = Object.freeze({ cpu: 'cpu', gpu: 'gpu', DEFAULT: 'cpu' });

export { Device };
